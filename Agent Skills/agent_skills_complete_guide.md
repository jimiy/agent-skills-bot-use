# 🚀 Agent Skills 完全入门指南：从Cursor小白到AI编程高手

> **适用对象**：Cursor/Google Antigravity用户，Agent Skills零基础小白  
> **目标**：手把手教你理解、安装、使用Agent Skills，并提供实战技巧

---

## 📋 目录

1. [什么是Agent Skills？（小白必看）](#1-什么是agent-skills小白必看)
2. [Agent Skills vs MCP：别再搞混了！](#2-agent-skills-vs-mcp别再搞混了)
3. [在Cursor中使用Agent Skills（详细步骤）](#3-在cursor中使用agent-skills详细步骤)
4. [在Google Antigravity中使用Agent Skills](#4-在google-antigravity中使用agent-skills)
5. [推荐Skills市场与下载](#5-推荐skills市场与下载)
6. [是否必须安装Claude？](#6-是否必须安装claude)
7. [从简单到复杂：实战案例](#7-从简单到复杂实战案例)
8. [注意事项与安全提醒](#8-注意事项与安全提醒)
9. [总结与进阶路线](#9-总结与进阶路线)

---

## 1. 什么是Agent Skills？（小白必看）

### 1.1 通俗理解

想象你请了一个**AI实习生**：

- **没有Skills时**：每次都要重新教TA怎么做，"请用Flutter写登录页"、"记得加const"、"用GetX而不是Provider"...重复一万遍😫
- **有了Skills后**：你写一本《公司Flutter开发手册》，AI实习生随时查阅，**一次编写，永久复用**！🎉

**Agent Skills = AI的"技能手册"或"工作说明书"**

### 1.2 核心特点

| 特点 | 说明 | 好处 |
|------|------|------|
| **📦 可复用** | 写好一次，所有项目都能用 | 不用重复教AI |
| **🎯 按需加载** | 需要时才调用，不占用资源 | 省Token，速度快 |
| **🔧 可移植** | 一个Skill，Cursor/Claude/Antigravity都能用 | 跨平台兼容 |
| **📚 标准化** | 统一格式（SKILL.md） | 社区共享方便 |

### 1.3 Skills长什么样？

```
my-skill/                    ← Skill文件夹
├── SKILL.md                 ← 核心文件（必须）
├── scripts/                 ← 可选：辅助脚本
│   └── setup.sh
├── templates/               ← 可选：模板文件
│   └── template.dart
└── resources/               ← 可选：其他资源
    └── logo.png
```

**SKILL.md文件示例**（简化版）：

```markdown
---
name: flutter-login-page
description: 创建符合公司规范的Flutter登录页面
---

# Flutter登录页Skill

## 使用场景
当用户需要创建Flutter登录页面时触发

## 规范要求
1. 必须使用GetX状态管理
2. 所有Widget必须加const构造函数
3. 颜色使用Theme.of(context).primaryColor
4. 表单验证使用validator插件

## 步骤
1. 创建lib/pages/login_page.dart
2. 使用LoginController管理状态
3. 实现邮箱+密码输入框
4. 添加"忘记密码"链接

## 示例代码
```dart
class LoginPage extends StatelessWidget {
  const LoginPage({Key? key}) : super(key: key);
  // ...
}
```
```

---

## 2. Agent Skills vs MCP：别再搞混了！

### 2.1 一句话区别

- **MCP（Model Context Protocol）**：让AI能**连接外部工具**的"USB接口"（如查数据库、读文件）
- **Agent Skills**：教AI**如何做事**的"说明书"（如怎么写代码、怎么做设计）

### 2.2 形象比喻

```
┌─────────────────────────────────────────────────────────────┐
│                        AI 厨师（Agent）                        │
├─────────────────────────────────────────────────────────────┤
│  MCP = 厨房设备（锅、刀、烤箱）                                │
│  Skills = 菜谱（宫保鸡丁怎么做、蛋糕怎么烤）                    │
└─────────────────────────────────────────────────────────────┘
```

**没有MCP**：厨师没工具，只能干瞪眼  
**没有Skills**：厨师有工具但不知道怎么做菜，乱做一气

### 2.3 详细对比表

| 维度 | MCP | Agent Skills |
|------|-----|--------------|
| **核心作用** | 提供"工具"（Tools） | 提供"知识/流程"（SOP） |
| **技术形态** | 服务器（Server）+ 协议 | Markdown文件（SKILL.md） |
| **解决的问题** | 怎么连外部系统 | 怎么做事更专业 |
| **举例** | 连接GitHub、数据库、文件系统 | Flutter开发规范、代码审查流程 |
| **Token占用** | 高（需要加载工具定义） | 低（按需加载） |
| **是否需要开发** | 需要写Server | 只需要写Markdown |
| **最佳实践** | **MCP + Skills 组合使用** | |

### 2.4 实际工作流程

```
用户请求："创建一个Flutter登录页"
        ↓
┌──────────────────────────────────────┐
│  Agent（AI）                          │
│  1. 识别需要"flutter-login" Skill     │
│  2. 加载SKILL.md文件                  │
└──────────────────────────────────────┘
        ↓
┌──────────────────────────────────────┐
│  Skill指导AI：                        │
│  - 使用GetX                           │
│  - 加const                            │
│  - 用Theme颜色                        │
└──────────────────────────────────────┘
        ↓
┌──────────────────────────────────────┐
│  AI决定调用MCP工具：                   │
│  - 调用FileSystem MCP创建文件         │
│  - 调用Browser MCP预览效果            │
└──────────────────────────────────────┘
        ↓
完成：符合规范的登录页代码
```

**结论**：
- ✅ MCP让AI有"手"（能操作外部系统）
- ✅ Skills让AI"会用手"（知道怎么做事）
- 💡 **两者结合 = 完美的AI助手**

---

## 3. 在Cursor中使用Agent Skills（详细步骤）

### 3.1 确认Cursor版本

**注意**：Skills功能在Cursor的**Nightly版本**或**0.45+版本**才支持

**检查方法**：
1. 打开Cursor
2. 点击左下角⚙️设置 → About
3. 查看版本号

**如果版本不够**：
- Windows/Mac：Cursor → Check for Updates
- 或访问 [cursor.com](https://cursor.com) 下载最新版

### 3.2 Skills存放位置

Cursor会从以下位置自动加载Skills：

| 位置 | 作用范围 | 适用场景 |
|------|----------|----------|
| `~/.cursor/skills/` | **全局**（所有项目） | 个人常用技能 |
| `.cursor/skills/` | **项目级**（当前项目） | 团队协作、项目专属 |

**路径说明**：
- `~` 表示用户主目录
  - Windows: `C:\Users\你的用户名\`
  - Mac: `/Users/你的用户名/`
  - Linux: `/home/你的用户名/`

### 3.3 安装Skills的4种方法

#### 方法一：使用npx skills命令（推荐⭐）

这是最简单的方法，一行命令搞定：

```bash
# 安装Vercel官方Skills
npx skills add vercel-labs/agent-skills

# 安装特定Skill
npx skills add yoanbernabeu/producthunt-skills --skill ph-launch-strategy

# 安装到全局（所有项目可用）
npx skills add vercel-labs/agent-skills -g

# 列出所有可用Skills
npx skills add vercel-labs/agent-skills --list
```

**优点**：自动识别Cursor目录，一键安装

#### 方法二：手动复制（适合学习）

```bash
# 1. 克隆Skills仓库
git clone https://github.com/example/my-skill.git

# 2. 复制到Cursor目录
# 全局安装
cp -r my-skill ~/.cursor/skills/

# 或项目级安装
cp -r my-skill ./.cursor/skills/
```

#### 方法三：使用VS Code插件（可视化）

1. 打开VS Code/Cursor
2. 点击左侧Extensions（扩展）图标
3. 搜索 "Agent Skills"
4. 安装插件
5. 点击左侧新出现的Skills图标，浏览市场并安装

#### 方法四：Git Submodule（团队协作）

```bash
# 将Skills作为子模块添加到项目
git submodule add https://github.com/team/skills.git .cursor/skills

# 团队成员克隆时
git submodule update --init --recursive
```

### 3.4 使用Skills

#### 自动触发

Cursor会自动识别何时使用Skill。例如你安装了"flutter-login" Skill，当你说：

> "帮我创建一个登录页面"

Cursor会自动：
1. 检测到需要Flutter登录页
2. 加载对应的Skill
3. 按照Skill规范生成代码

#### 手动触发

在Chat中输入 `/` 查看可用Skills：

```
/技能名称

# 例如
/flutter-login
```

或在提示词中明确说明：

> "使用flutter-login skill创建登录页"

### 3.5 创建自己的Skill（实战）

#### 步骤1：创建目录

```bash
# 创建Skill目录
mkdir -p ~/.cursor/skills/my-company-flutter/SKILL.md
```

#### 步骤2：编写SKILL.md

```markdown
---
name: my-company-flutter
description: 我公司Flutter开发规范，包含状态管理、UI组件、代码风格
---

# 公司Flutter开发规范

## 技术栈
- 状态管理：GetX
- 网络请求：Dio + Retrofit
- 本地存储：GetStorage
- UI组件：自定义组件库

## 代码规范
1. 所有Widget必须加const
2. 颜色必须从Theme获取，禁止硬编码
3. 字符串必须国际化
4. 每个页面必须有对应的Controller

## 文件结构
```
lib/
├── pages/           # 页面
├── controllers/     # 控制器
├── models/          # 数据模型
├── services/        # 服务层
├── widgets/         # 公共组件
└── utils/           # 工具类
```

## 创建新页面步骤
1. 在pages/创建page文件
2. 在controllers/创建对应controller
3. 在bindings/添加依赖注入
4. 更新路由

## 示例
```dart
// pages/home_page.dart
class HomePage extends StatelessWidget {
  const HomePage({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('home_title'.tr)),
      body: const Center(child: Text('Hello')),
    );
  }
}
```
```

#### 步骤3：测试Skill

1. 重启Cursor（让Cursor重新加载Skills）
2. 打开Chat
3. 输入："使用my-company-flutter skill创建首页"
4. 观察AI是否按照规范生成代码

### 3.6 Cursor Skills设置界面

**打开方式**：`Cursor Settings → Rules, Skills, and Agent`

在这个界面你可以：
- ✅ 查看已安装的Skills
- ✅ 启用/禁用特定Skill
- ✅ 查看Skill文件路径
- ✅ 快速跳转到Skill目录

---

## 4. 在Google Antigravity中使用Agent Skills

### 4.1 Google Antigravity简介

**Google Antigravity**是Google推出的**Agent-First IDE**（2025年11月发布），特点：

- 🤖 **多Agent并行**：可同时运行多个AI Agent
- 🌐 **浏览器集成**：Agent能自动打开浏览器测试
- 📝 **Artifacts机制**：自动生成任务计划、截图、报告
- 🔄 **Planning Mode**：AI先规划再执行，可审查每一步

### 4.2 Antigravity中的Skills

Antigravity同样支持Agent Skills标准，目录结构：

| 位置 | 作用范围 |
|------|----------|
| `~/.antigravity/skills/` | 全局 |
| `.antigravity/skills/` | 项目级 |

### 4.3 在Antigravity中安装Skills

#### 方法一：npx skills（推荐）

```bash
# 在Antigravity终端中运行
npx skills add vercel-labs/agent-skills

# 或指定Antigravity
npx skills add vercel-labs/agent-skills -a antigravity
```

#### 方法二：使用add-skill工具

```bash
npx add-skill vercel-labs/agent-skills
```

**add-skill特点**：自动检测Antigravity并安装到正确目录

#### 方法三：手动安装

```bash
# 克隆仓库
git clone https://github.com/example/skills.git

# 复制到Antigravity目录
cp -r skills/* ~/.antigravity/skills/
```

### 4.4 Antigravity中使用Skills的特点

**与Cursor的区别**：

| 特性 | Cursor | Antigravity |
|------|--------|-------------|
| 触发方式 | Chat输入或自动检测 | Manager View分配 |
| 可视化 | 较弱 | 强（Manager View看所有Agent） |
| 多Agent | 不支持 | 支持并行 |
| 浏览器测试 | 需配合MCP | 内置支持 |

**在Antigravity中使用步骤**：

1. **打开Manager View**（左侧边栏）
2. **创建新任务**：描述需求
3. **分配Agent**：指定使用哪个Skill
4. **审查计划**：在Planning Mode下查看AI的执行计划
5. **批准执行**：确认后AI开始工作
6. **查看Artifacts**：查看生成的文档、截图等

### 4.5 Antigravity + Skills实战示例

**场景**：创建一个电商网站

**步骤**：

1. **安装Web开发Skills**：
   ```bash
   npx skills add vercel-labs/agent-skills
   ```

2. **在Manager View创建任务**：
   ```
   任务：创建电商网站首页
   要求：
   - 使用Next.js + Tailwind CSS
   - 包含商品列表、购物车图标、轮播图
   - 响应式设计
   ```

3. **Antigravity自动**：
   - 创建项目结构
   - 生成页面代码
   - 打开浏览器预览
   - 生成测试报告

4. **查看Artifacts**：
   - `task-plan.md`：执行计划
   - `implementation.md`：实现细节
   - `screenshot.png`：效果截图

---

## 5. 推荐Skills市场与下载

### 5.1 官方/权威Skills仓库

| 仓库 | 描述 | 安装命令 |
|------|------|----------|
| [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills) | Vercel官方Skills | `npx skills add vercel-labs/agent-skills` |
| [anthropics/skills](https://github.com/anthropics/skills) | Anthropic官方 | `npx skills add anthropics/skills` |
| [yoanbernabeu/producthunt-skills](https://github.com/yoanbernabeu/producthunt-skills) | Product Hunt发布 | `npx skills add yoanbernabeu/producthunt-skills` |
| [chirag2653/free-ai-agent-skills](https://github.com/chirag2653/free-ai-agent-skills) | 免费精选 | `npx skills add chirag2653/free-ai-agent-skills` |

### 5.2 分类推荐

#### 🚀 开发类

| Skill | 用途 | 安装 |
|-------|------|------|
| **frontend-design** | 前端设计规范 | `npx skills add anthropic/skills/frontend-design` |
| **backend-api** | 后端API开发 | `npx skills add vercel-labs/agent-skills:backend-api` |
| **testing** | 测试编写 | `npx skills add chirag2653/free-ai-agent-skills --skill testing` |
| **code-review** | 代码审查 | `npx skills add coderabbitai/skills` |

#### 📱 移动端

| Skill | 用途 | 安装 |
|-------|------|------|
| **flutter-dev** | Flutter开发 | `npx skills add awesome-flutter/skills` |
| **react-native** | React Native | `npx skills add vercel-labs/agent-skills:react-native` |
| **ios-dev** | iOS开发 | `npx skills add awesome-ios/skills` |

#### 🎨 设计/创意类

| Skill | 用途 | 安装 |
|-------|------|------|
| **ui-design** | UI设计 | `npx skills add design-system/skills` |
| **icon-generation** | 图标生成 | `npx skills add awesome-design/skills --skill icons` |

#### 📝 内容/营销类

| Skill | 用途 | 安装 |
|-------|------|------|
| **content-creator** | 内容创作 | `npx skills add nicepkg/ai-workflow/workflows/content-creator-workflow` |
| **marketing-pro** | 营销专业 | `npx skills add coreyhaines31/marketingskills` |
| **seo-optimization** | SEO优化 | `npx skills add marketing-skills/seo` |

#### 🔧 DevOps/工具类

| Skill | 用途 | 安装 |
|-------|------|------|
| **docker-setup** | Docker配置 | `npx skills add devops-skills/docker` |
| **ci-cd** | CI/CD流程 | `npx skills add devops-skills/ci-cd` |
| **aws-deployment** | AWS部署 | `npx skills add vercel-labs/agent-skills:aws` |

### 5.3 Skills市场平台

| 平台 | 网址 | 特点 |
|------|------|------|
| **skills.sh** | [skills.sh](https://skills.sh) | Vercel官方市场，可搜索、排行 |
| **SkillzWave** | [skillzwave.ai](https://skillzwave.ai) | 44,000+ Skills，分类详细 |
| **Agent Skills Marketplace** | VS Code插件 | 可视化浏览安装 |
| **GitHub Topics** | GitHub搜索`agent-skills` | 社区开源Skills |

### 5.4 搜索Skills的技巧

在GitHub搜索：
```
agent-skills topic:cursor
agent-skills topic:claude-code
agent-skills topic:flutter
```

或使用skills CLI搜索：
```bash
# 安装skills CLI
npm install -g agent-skills-cli

# 搜索Skills
skills search react
skills search flutter

# 交互式搜索
skills search -i
```

---

## 6. 是否必须安装Claude？

### 6.1 直接回答

**不需要！** 🎉

Agent Skills是**开放标准**，不是Claude专属。支持的平台：

| 平台 | 支持状态 | 备注 |
|------|----------|------|
| **Cursor** | ✅ 完全支持 | 推荐，IDE体验好 |
| **Google Antigravity** | ✅ 完全支持 | Agent-first，多Agent并行 |
| **Claude Code** | ✅ 原生支持 | Anthropic官方 |
| **GitHub Copilot** | ✅ 支持 | VS Code中可用 |
| **OpenAI Codex** | ✅ 支持 | OpenAI官方CLI |
| **Windsurf** | ✅ 支持 | Codeium出品 |
| **OpenCode** | ✅ 支持 | 开源AI IDE |
| **Gemini CLI** | ✅ 支持 | Google官方 |

### 6.2 各平台对比

| 平台 | 适合人群 | 优点 | 缺点 |
|------|----------|------|------|
| **Cursor** | 大多数开发者 | 成熟稳定、生态好、VS Code兼容 | 付费（有免费额度） |
| **Antigravity** | 尝鲜者、Agent爱好者 | 免费、多Agent、创新 | 预览版、偶尔不稳定 |
| **Claude Code** | Claude用户 | 原生支持、深度集成 | 需要Claude订阅 |
| **Windsurf** | 预算有限者 | 免费额度多、速度快 | 功能相对简单 |

### 6.3 推荐选择

- **新手入门**：Cursor（免费版够用）
- **深度使用**：Cursor Pro 或 Antigravity
- **团队协作**：Cursor + 项目级Skills
- **预算有限**：Windsurf 或 Antigravity

---

## 7. 从简单到复杂：实战案例

### 7.1 入门级：创建个人Skill（5分钟）

**目标**：创建一个"Python代码规范" Skill

**步骤**：

1. **创建目录**：
   ```bash
   mkdir -p ~/.cursor/skills/python-style-guide
   ```

2. **创建SKILL.md**：
   ```markdown
   ---
   name: python-style-guide
   description: 个人Python代码规范，包含PEP8、类型注解、文档字符串
   ---
   
   # Python代码规范
   
   ## 基本要求
   1. 遵循PEP 8
   2. 使用类型注解
   3. 函数必须有docstring
   4. 使用black格式化
   
   ## 函数模板
   ```python
   def calculate_total(
       items: list[dict],
       tax_rate: float = 0.1
   ) -> float:
       """
       计算订单总价
       
       Args:
           items: 商品列表，每个商品包含price和quantity
           tax_rate: 税率，默认0.1
           
       Returns:
           含税总价
       """
       subtotal = sum(item['price'] * item['quantity'] for item in items)
       return subtotal * (1 + tax_rate)
   ```
   ```

3. **测试**：
   - 重启Cursor
   - 输入："用python-style-guide skill写一个计算BMI的函数"
   - 检查是否符合规范

### 7.2 进阶级：团队共享Skill（15分钟）

**目标**：创建团队Flutter开发规范，并共享给团队成员

**步骤**：

1. **创建Git仓库**：
   ```bash
   # 在GitHub/GitLab创建仓库 team-flutter-skills
   git clone https://github.com/your-team/team-flutter-skills.git
   cd team-flutter-skills
   ```

2. **创建Skill结构**：
   ```
   team-flutter-skills/
   ├── skills/
   │   ├── flutter-architecture/
   │   │   └── SKILL.md
   │   ├── flutter-ui-components/
   │   │   └── SKILL.md
   │   └── flutter-state-management/
   │       └── SKILL.md
   ├── templates/
   │   ├── page_template.dart
   │   └── controller_template.dart
   └── README.md
   ```

3. **编写SKILL.md**（flutter-architecture示例）：
   ```markdown
   ---
   name: flutter-architecture
   description: 团队Flutter架构规范，包含项目结构、依赖管理、路由配置
   ---
   
   # Flutter架构规范
   
   ## 项目结构
   ```
   lib/
   ├── app/                    # 应用层
   │   ├── routes/            # 路由配置
   │   ├── bindings/          # 依赖注入
   │   └── middleware/        # 中间件
   ├── modules/               # 业务模块
   │   ├── home/
   │   │   ├── pages/
   │   │   ├── controllers/
   │   │   ├── widgets/
   │   │   └── bindings/
   │   └── profile/
   ├── shared/                # 公共资源
   │   ├── widgets/          # 公共组件
   │   ├── utils/            # 工具类
   │   └── constants/        # 常量
   └── main.dart
   ```
   
   ## 依赖管理
   ```yaml
   dependencies:
     flutter:
       sdk: flutter
     get: ^4.6.6
     dio: ^5.4.0
     get_storage: ^2.1.1
   ```
   
   ## 创建新模块步骤
   1. 在modules/创建模块目录
   2. 创建pages、controllers、widgets、bindings子目录
   3. 在routes中添加路由
   4. 在bindings中添加依赖注入
   ```

4. **团队成员使用**：
   ```bash
   # 方式1：作为Git子模块
   git submodule add https://github.com/your-team/team-flutter-skills.git .cursor/team-skills
   
   # 方式2：使用npx skills
   npx skills add your-team/team-flutter-skills
   
   # 方式3：手动克隆
   git clone https://github.com/your-team/team-flutter-skills.git
   cp -r team-flutter-skills/skills/* .cursor/skills/
   ```

### 7.3 高级：Skill + MCP组合使用（30分钟）

**目标**：创建一个"智能代码审查"系统

**架构**：
```
┌─────────────────────────────────────────────┐
│  用户："审查这个PR"                           │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  Skill：code-review-skill                    │
│  - 定义审查流程                               │
│  - 指定检查项                                 │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  AI Agent                                    │
│  1. 分析代码变更                              │
│  2. 决定调用哪些工具                          │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  MCP工具：                                   │
│  - GitHub MCP：获取PR详情、diff              │
│  - FileSystem MCP：读取本地代码              │
│  - Browser MCP：预览效果                     │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  输出：审查报告                               │
│  - 代码问题                                   │
│  - 改进建议                                   │
│  - 截图对比                                   │
└─────────────────────────────────────────────┘
```

**实现步骤**：

1. **安装MCP Server**（GitHub MCP示例）：
   ```json
   // ~/.cursor/mcp.json
   {
     "mcpServers": {
       "github": {
         "command": "npx",
         "args": ["-y", "@modelcontextprotocol/server-github"],
         "env": {
           "GITHUB_PERSONAL_ACCESS_TOKEN": "your-token"
         }
       }
     }
   }
   ```

2. **创建Code Review Skill**：
   ```markdown
   ---
   name: code-review-skill
   description: 自动化代码审查，结合GitHub MCP获取PR信息
   ---
   
   # 代码审查流程
   
   ## 触发条件
   当用户要求审查代码或PR时
   
   ## 审查步骤
   1. **获取PR信息**
      - 使用GitHub MCP获取PR详情
      - 获取变更文件列表
      - 获取diff内容
   
   2. **代码分析**
      - 检查代码风格
      - 检查潜在bug
      - 检查安全漏洞
      - 检查性能问题
   
   3. **生成报告**
      - 总结变更内容
      - 列出发现的问题
      - 提供改进建议
      - 给出整体评价
   
   ## 检查清单
   - [ ] 代码是否符合团队规范
   - [ ] 是否有足够的单元测试
   - [ ] 是否存在SQL注入风险
   - [ ] 是否有性能瓶颈
   - [ ] 文档是否更新
   
   ## 输出格式
   ```markdown
   ## PR审查报告
   
   ### 概述
   - PR编号：#123
   - 作者：@username
   - 变更文件：5个
   
   ### 发现的问题
   1. **高优先级**
      - [文件路径] 问题描述
   
   2. **中优先级**
      - [文件路径] 问题描述
   
   3. **建议**
      - 改进建议
   
   ### 总体评价
   ✅ / ⚠️ / ❌ （通过/需修改/拒绝）
   ```
   ```

3. **使用**：
   ```
   "使用code-review-skill审查PR #123"
   ```

### 7.4 专家级：创建可执行的Skill（带脚本）

**目标**：创建一个"自动部署" Skill，包含实际执行的脚本

**结构**：
```
auto-deploy/
├── SKILL.md
└── scripts/
    ├── build.sh
    ├── test.sh
    └── deploy.sh
```

**SKILL.md**：
```markdown
---
name: auto-deploy
description: 自动化构建、测试、部署流程
---

# 自动部署流程

## 使用场景
当需要部署应用时触发

## 部署步骤
1. **构建**
   ```bash
   ./scripts/build.sh
   ```
   
2. **测试**
   ```bash
   ./scripts/test.sh
   ```
   
3. **部署**
   ```bash
   ./scripts/deploy.sh
   ```

## 环境要求
- Node.js 18+
- Docker
- kubectl（用于K8s部署）

## 配置
在.env文件中设置：
```
DEPLOY_ENV=production
KUBE_CONFIG_PATH=/path/to/config
```
```

**scripts/build.sh**：
```bash
#!/bin/bash
set -e

echo "🚀 开始构建..."

# 安装依赖
npm ci

# 构建
npm run build

# 构建Docker镜像
docker build -t myapp:$VERSION .

echo "✅ 构建完成"
```

**使用**：
```
"使用auto-deploy skill部署到生产环境"
```

---

## 8. 注意事项与安全提醒

### 8.1 ⚠️ 安全注意事项

#### Skill来源安全

| 风险 | 说明 | 防护措施 |
|------|------|----------|
| **恶意脚本** | Skill中的scripts可能包含危险命令 | 只安装可信来源的Skill |
| **代码注入** | 恶意Skill可能注入后门代码 | 审查SKILL.md内容 |
| **权限过大** | Skill可能请求不必要的权限 | 使用最小权限原则 |

**安全实践**：
1. ✅ 优先使用官方或知名社区的Skill
2. ✅ 安装前阅读SKILL.md内容
3. ✅ 审查scripts/目录下的脚本
4. ❌ 不要安装来源不明的Skill
5. ❌ 不要在Skill中硬编码敏感信息（API Key、密码）

#### MCP安全

```json
// ❌ 错误：在配置中暴露密钥
{
  "mcpServers": {
    "database": {
      "env": {
        "DB_PASSWORD": "123456"  // 危险！
      }
    }
  }
}

// ✅ 正确：使用环境变量
{
  "mcpServers": {
    "database": {
      "env": {
        "DB_PASSWORD": "${DB_PASSWORD}"  // 从环境变量读取
      }
    }
  }
}
```

### 8.2 💡 使用技巧

#### Token管理

- **问题**：加载太多Skill会占用大量Token
- **解决**：
  - 按需启用Skill（在设置中禁用不用的）
  - 使用项目级Skill而非全局Skill
  - 精简SKILL.md内容，只保留核心指令

#### 版本管理

```bash
# 使用Git管理Skills
# 在项目中创建.skills目录作为子模块
git submodule add https://github.com/team/skills.git .skills

# 锁定版本
cd .skills
git checkout v1.2.3

# 更新Skills
git submodule update --remote
```

#### 调试技巧

**Skill不生效？**
1. 检查目录结构是否正确（必须包含SKILL.md）
2. 重启Cursor/Antigravity
3. 检查Skill是否被禁用（Settings中查看）
4. 查看输出日志（View → Output → MCP）

**查看已加载的Skills**：
- Cursor：`/` 命令查看列表
- Claude Code：`/mcp` 命令

### 8.3 ❌ 常见错误

| 错误 | 原因 | 解决 |
|------|------|------|
| Skill未找到 | 目录名或文件名错误 | 确保是`SKILL.md`，不是`skill.md` |
| Skill未加载 | 目录结构错误 | 必须是`skill-name/SKILL.md` |
| 中文乱码 | 文件编码问题 | 使用UTF-8编码保存 |
| 权限不足 | 脚本没有执行权限 | `chmod +x scripts/*.sh` |

---

## 9. 总结与进阶路线

### 9.1 核心要点回顾

```
┌─────────────────────────────────────────────────────────────┐
│                    Agent Skills 核心知识                      │
├─────────────────────────────────────────────────────────────┤
│  1. Skills是AI的"说明书"，让AI按规范工作                      │
│  2. 格式：SKILL.md（YAML frontmatter + Markdown）            │
│  3. 位置：~/.cursor/skills/（全局）或 .cursor/skills/（项目）  │
│  4. 安装：npx skills add 仓库地址                             │
│  5. 使用：AI自动检测或手动 /skill-name                        │
│  6. MCP是"工具"，Skills是"知识"，两者结合最佳                  │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 学习路线

```
第1周：入门
├── 安装Cursor或Antigravity
├── 安装2-3个官方Skills体验
├── 创建一个简单的个人Skill
└── 理解Skills vs MCP的区别

第2周：进阶
├── 创建团队共享Skill
├── 学习Skill + MCP组合使用
├── 探索Skills市场
└── 阅读优秀Skill源码

第3周：实战
├── 为工作流创建专用Skills
├── 建立团队Skills仓库
├── 优化Skill性能（Token管理）
└── 分享Skills到社区

第4周：精通
├── 开发带脚本的复杂Skill
├── 贡献开源Skills
├── 建立企业级Skills体系
└── 成为社区专家
```

### 9.3 推荐资源

**官方文档**：
- [Cursor Skills文档](https://cursor.com/docs/context/skills)
- [Anthropic Skills规范](https://docs.anthropic.com/)
- [skills.sh市场](https://skills.sh)

**GitHub仓库**：
- [awesome-agent-skills](https://github.com/JackyST0/awesome-agent-skills) - 精选列表
- [free-ai-agent-skills](https://github.com/chirag2653/free-ai-agent-skills) - 免费Skills
- [vercel-labs/agent-skills](https://github.com/vercel-labs/agent-skills) - 官方示例

**社区**：
- Cursor社区论坛
- Reddit r/cursor
- Discord AI编程频道

### 9.4 最后的话

**Agent Skills是AI编程的未来趋势**。它让AI从"通用助手"变成"专业专家"，从"每次重教"变成"一次学会"。

**记住三个关键词**：
1. **标准化**（SKILL.md格式）
2. **可复用**（一次编写，到处使用）
3. **按需加载**（省Token，更高效）

**现在就开始**：
1. 打开Cursor或Antigravity
2. 安装第一个Skill：`npx skills add vercel-labs/agent-skills`
3. 创建你的第一个Skill
4. 享受AI按你规范工作的快感！🚀

---

## 📎 附录

### A. 快速命令参考

```bash
# 安装Skills
npx skills add 仓库地址                    # 基础安装
npx skills add 仓库地址 -g                 # 全局安装
npx skills add 仓库地址 --skill 名称       # 安装特定Skill
npx skills add 仓库地址 -a cursor          # 指定平台
npx skills add 仓库地址 --list             # 列出可用Skills

# 使用add-skill
npx add-skill 仓库地址                     # 自动检测平台
npx add-skill 仓库地址 --global            # 全局安装

# 其他工具
npm install -g agent-skills-cli           # 安装CLI
skills search 关键词                       # 搜索Skills
skills install 名称                        # 安装Skills

# 手动安装
git clone 仓库地址
cp -r skills/* ~/.cursor/skills/
```

### B. SKILL.md模板

```markdown
---
name: skill-name
description: 简短描述这个Skill的功能
author: 你的名字
version: "1.0"
tags:
  - tag1
  - tag2
---

# Skill名称

## 描述
详细描述这个Skill的作用

## 使用场景
- 场景1：什么时候使用
- 场景2：触发条件

## 前置条件
- 需要什么环境
- 需要什么依赖

## 步骤/指令
1. 第一步做什么
2. 第二步做什么
3. 第三步做什么

## 示例
### 输入
用户请求示例

### 输出
预期结果

## 注意事项
- 注意点1
- 注意点2

## 相关资源
- [链接1](url)
- [链接2](url)
```

### C. 平台配置路径速查

| 平台 | 全局路径 | 项目路径 | MCP配置 |
|------|----------|----------|---------|
| **Cursor** | `~/.cursor/skills/` | `.cursor/skills/` | `.cursor/mcp.json` |
| **Claude Code** | `~/.claude/skills/` | `.claude/skills/` | `~/.config/claude/claude_desktop_config.json` |
| **Antigravity** | `~/.antigravity/skills/` | `.antigravity/skills/` | `.antigravity/mcp.json` |
| **Codex** | `~/.codex/skills/` | `.codex/skills/` | `~/.config/codex/mcp.json` |
| **Windsurf** | `~/.windsurf/skills/` | `.windsurf/skills/` | `.windsurf/mcp.json` |

---

**🎉 恭喜你读完了这份指南！现在去实践吧！**

如有问题，欢迎在社区讨论或查阅官方文档。

**Happy Coding with Agent Skills!** 🚀
